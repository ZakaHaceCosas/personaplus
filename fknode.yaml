divineProtection: "updater"
lintCmd: "lint"
prettyCmd: "prettify"
destroy:
    intensities: [ "*" ]
    targets:
      - ".expo/"
commitActions: true
commitMessage: "(chore) code quality task (automated by F*ckingNode)"
